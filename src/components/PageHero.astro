---
type Accent = 'green' | 'sky' | 'grape' | 'coral' | 'tangerine' | 'bubblegum' | 'sunny';

interface Props {
  eyebrow?: string;
  title: string;
  accent?: Accent;
}

// Gradients reference the brand tokens so they track the logo palette in tokens.css.
const accentMap: Record<Accent, { a: string; b: string; ink: string }> = {
  green: { a: 'var(--c-secondary)', b: 'var(--c-sky)', ink: '#fff' },
  sky: { a: 'var(--c-sky)', b: 'var(--c-grape)', ink: '#fff' },
  grape: { a: 'var(--c-grape)', b: 'var(--c-bubblegum)', ink: '#fff' },
  coral: { a: 'var(--c-coral)', b: 'var(--c-tangerine)', ink: '#fff' },
  tangerine: { a: 'var(--c-tangerine)', b: 'var(--c-primary)', ink: 'var(--c-tangerine-ink)' },
  bubblegum: { a: 'var(--c-bubblegum)', b: 'var(--c-grape)', ink: '#fff' },
  sunny: { a: 'var(--c-primary)', b: 'var(--c-tangerine)', ink: 'var(--c-primary-ink)' },
};

const { eyebrow, title, accent = 'green' } = Astro.props;
const c = accentMap[accent];
---

<section class="page-hero" style={`--h-a:${c.a}; --h-b:${c.b}; --h-ink:${c.ink}`}>
  <div class="page-hero__blobs" aria-hidden="true">
    <span></span><span></span><span></span>
  </div>
  <div class="container page-hero__inner">
    {eyebrow && <span class="page-hero__eyebrow">{eyebrow}</span>}
    <h1 class="page-hero__title">{title}</h1>
    <slot />
  </div>
</section>

<style>
  .page-hero {
    position: relative;
    isolation: isolate;
    overflow: hidden;
    background: linear-gradient(120deg, var(--h-a), var(--h-b));
    color: var(--h-ink);
    padding-block: clamp(3rem, 8vw, 5rem);
    text-align: center;
  }
  .page-hero__inner {
    position: relative;
    z-index: 2;
    max-width: 46rem;
  }
  .page-hero__eyebrow {
    display: inline-block;
    font-family: var(--font-display);
    font-weight: var(--fw-bold);
    font-size: var(--fs-sm);
    letter-spacing: 0.06em;
    text-transform: uppercase;
    opacity: 0.9;
    margin-bottom: var(--sp-2);
  }
  .page-hero__title {
    color: var(--h-ink);
    font-size: var(--fs-900);
  }
  .page-hero :global(p) {
    margin-top: var(--sp-3);
    font-size: var(--fs-600);
    color: var(--h-ink);
    opacity: 0.94;
  }
  .page-hero__blobs span {
    position: absolute;
    border-radius: 50%;
    background: rgba(255, 255, 255, 0.16);
    filter: blur(2px);
  }
  .page-hero__blobs span:nth-child(1) {
    width: 220px;
    height: 220px;
    top: -80px;
    right: -40px;
  }
  .page-hero__blobs span:nth-child(2) {
    width: 140px;
    height: 140px;
    bottom: -60px;
    left: 8%;
  }
  .page-hero__blobs span:nth-child(3) {
    width: 90px;
    height: 90px;
    top: 30%;
    left: -30px;
    background: rgba(255, 255, 255, 0.1);
  }
</style>
